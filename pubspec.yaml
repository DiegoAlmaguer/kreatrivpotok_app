name: kreativ_potok_app
description: "Kreativ Potok mobile app"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  # Routing
  go_router: ^14.2.0

  # Backend
  supabase_flutter: ^2.12.0

  # Utils
  intl: ^0.19.0
  url_launcher: ^6.3.0

  # UI
  cupertino_icons: ^1.0.8
  google_fonts: ^6.3.0
  flutter_svg: ^2.2.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
